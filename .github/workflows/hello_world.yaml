

# name
name: hello_world

# trigger
on:
- push

# one or more jobs
jobs:
    greetings:
        runs-on: ubuntu-latest
        steps:
        - id: s00
        # running some shell bash command
          run: echo "Current time in GitHub is $(date), $(GITHUB_OUTPUT)"
        # Create a value and pass it to the next step
        - id: s01
        # redirecting output to a file
        # single > overwrites, double >> appends
          run: echo "CURR_DATE=$(date)" >> $GITHUB_OUTPUT
        - id: s02
          uses: mscoutermarsh/cowsays-action@master
          with: 
            text: 'Moo moo cow! ${{steps.s01.outputs.CURR_DATE}}, ${{github.ref}}, ${{github.repository}}'
            color: 'green'
        # - id: s01
        #   run: ls -al
        # - id: s02
        #   run: cat hello.txt